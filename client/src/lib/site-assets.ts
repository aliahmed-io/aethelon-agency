import { siteImages } from "../../../shared/projects";

// Backward-compatible alias for presentation components. Asset definitions live
// with the shared project registry so routes and cards cannot drift apart.
export const IMG = siteImages;
